include includes/head

body

    form(action=`/van`, method="post")

        div#div-create-van
        div
            h2 Opret Lagerbil
        div 
            label(for="input-licensePlate") Registreringsnummer:
            input.input-vanLicensePlate(type="text" name="licensePlate")

            label(for="input-vanOwner") Navn: 
            input.input-vanOwner(type="text" name="owner")

        button(id="create-van-btn" class="button-create-van" type="submit") Opret Lagerbil