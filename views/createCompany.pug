include includes/head

body
    button
        i.fa-sharp.fa-solid.fa-left-long
        a.knap(href='/login') Tilbage

    form(action="/company", method='post')

        div#div-createCompany
        div
            h2 Opret Firma
        div
            label(for="input-company-name") Firma navn:
            input.inputtag(type="text" name="input-company-name")

            label(for="input-cvr-nr") CVR nr: 
            input.inputtag(type="text" name="input-cvr-nr")

            label(for="contact-person") Kontaktperson:
            input.inputtag(type="text" name="contact-person")

            label(for="contact-person-number") Kontaktperson tlf. nr.:
            input.inputtag(type="text" name="contact-person-number")

        button(id="create-company-btn" class="create-company" type="submit") Opret Firma
            