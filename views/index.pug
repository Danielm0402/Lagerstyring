include includes/head

body
    h1 Bil:
        select(name='my-dropdown')
            each option in ['Olivers Bil', 'Bil 2', 'Bil 3']
                option(value=option)= option
    


    button.admin 
        a(href="/admin") Admin
    

    each vare in varer
        div.varebokse
                p #{vare.Varenavn}
                p På lager: 5
                div.knapper
                    button.delete-button(type='button')
                        ion-icon(src="icons/trash-outline.svg")
                     button.plusminus(type='button', data-vareid=vare.id, on-click='justerAntal(this, "op")') +
      button.plusminus(type='button', data-vareid=vare.id, on-click='justerAntal(this, "ned")') -

    button.addProduct
        a(href="/form") Tilføj ny vare


    include includes/createProduct